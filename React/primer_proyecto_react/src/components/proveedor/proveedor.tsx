export function Proveedor({name}){ //nombre de la clave en llaves "{}"
    return(
        <> 
            export nombrado
            <p>{name}</p> 
        </>
    ) // Usamos llave para que no lo tome como texto plano
};